'use strict';

require('react');
var index = require('../../prop-types/index.js');
require('./Animate.js');

({
  appearOptions: index.object,
  enterOptions: index.object,
  leaveOptions: index.object,
  children: index.element
});
