'use strict';

require('react');
var index = require('../../prop-types/index.js');
require('./AnimateGroupChild.js');

({
  appear: index.object,
  enter: index.object,
  leave: index.object,
  children: index.oneOfType([index.array, index.element]),
  component: index.any
});
