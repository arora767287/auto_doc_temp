"use client";
'use strict';

var _tslib = require('../_virtual/_tslib.js');
var index = require('../external/classnames/index.js');
var React = require('react');
var index$1 = require('../assets/icons/final/index.js');
var icon_module = require('./icon.module.css.js');

var Icon = React.forwardRef(function Icon(props, ref) {
    var className = props.className, name = props.name, otherProps = _tslib.__rest(props, ["className", "name"]);
    if (name === null) {
        return null;
    }
    var children;
    if (name === "empty") {
        children = React.createElement("svg", { width: "1em", height: "1em" });
    }
    else {
        var Component = index$1[name];
        // TODO warn?
        if (!Component) {
            console.warn("Icon rendered with invalid name: ".concat(name));
            return null;
        }
        children = React.createElement(Component, null);
    }
    return (React.createElement("span", _tslib.__assign({ className: index(className, icon_module.root), ref: ref }, otherProps), children));
});

exports.Icon = Icon;
