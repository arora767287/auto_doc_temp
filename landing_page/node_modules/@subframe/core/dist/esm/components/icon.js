"use client";
import { __rest, __assign } from '../_virtual/_tslib.js';
import classNames from '../external/classnames/index.js';
import React__default from 'react';
import * as index from '../assets/icons/final/index.js';
import styles from './icon.module.css.js';

var Icon = React__default.forwardRef(function Icon(props, ref) {
    var className = props.className, name = props.name, otherProps = __rest(props, ["className", "name"]);
    if (name === null) {
        return null;
    }
    var children;
    if (name === "empty") {
        children = React__default.createElement("svg", { width: "1em", height: "1em" });
    }
    else {
        var Component = index[name];
        // TODO warn?
        if (!Component) {
            console.warn("Icon rendered with invalid name: ".concat(name));
            return null;
        }
        children = React__default.createElement(Component, null);
    }
    return (React__default.createElement("span", __assign({ className: classNames(className, styles.root), ref: ref }, otherProps), children));
});

export { Icon };
